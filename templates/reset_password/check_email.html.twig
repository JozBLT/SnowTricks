{% extends 'base.html.twig' %}

{% block title %}Email envoyé{% endblock %}

{% block body %}
    <div class="w-full max-w-lg mx-auto my-16 bg-white p-8 rounded-lg shadow-md text-center">
        <h1 class="text-2xl font-bold text-center mb-8">Email envoyé</h1>

        <p class="text-gray-700 mb-4">
            Si un compte existe avec l'adresse email renseignée,
            un lien vous a été envoyé par mail pour réinitialiser votre mot de passe.
        </p>

        <p class="text-sm text-gray-600 mb-6">
            Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
        </p>

        <p class="text-gray-700">
            Si vous ne recevez pas le mail, pensez à vérifier vos spams !
            Sinon, vous pouvez <a href="{{ path('app_forgot_password_request') }}" class="text-blue-500 underline">réessayer</a>.
        </p>
    </div>
{% endblock %}
