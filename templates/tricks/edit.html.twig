{% extends 'base.html.twig' %}

{% block title "Édition de Trick : " ~ trick.title %}

{% block body %}
    <div class="example-wrapper">
        <h1>{{ trick.title }}</h1>

        {{ form(editForm) }}
    </div>
{% endblock %}
